 <h1 align="center">User Profile <%= @user.username %></h1>
 <div class="row">
    <div class="col-md-4 col-md-offset-5 center">
         <%-#this method isnt defined right now so we are going to define it in the helper function-%>
        <%= gravatar_for @user%>                  
    </div>
</div>
<h4 align="center"><%= @user.username %> articles</h4>
<div class="center">
    <%= will_paginate @user_articles%>
</div>
<%= render 'articles/article' , obj: @user_articles %>
<div class="center">
    <%= will_paginate @user_articles%>
</div>